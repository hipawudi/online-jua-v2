'use strict';

/**
 * discipline-commission controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::discipline-commission.discipline-commission');
